plan_id: "20260603-fun-sejmapi"
objective: "Prepare executable CI task breakdown for new fun_sejmapi module"
focus_area: "GitHub Actions workflow for module build and test"
created_at: "2026-06-03"
created_by: "GitHub Copilot GPT-5.4"
status: "completed"
tldr: |
  - The repo currently has one module-scoped GitHub Actions workflow, `.github/workflows/fun_sejmlive-ci.yml`, and it establishes the only in-repo CI pattern for path filters, `mvn -pl <module> -am test`, retry logic, and artifact upload.
  - The monorepo root currently declares only `fun_sejmlive` in the Maven aggregator, so `fun_sejmapi` CI cannot execute until the root `pom.xml` includes that module.
  - Approved kickoff decisions require a dedicated workflow for `fun_sejmapi`, Java 21, path-scoped triggering, and artifacts relevant to Azure Functions packaging rather than a Spring Boot jar.
  - No `fun_sejmapi` module, Azure Functions Maven plugin, or existing Azure Functions workflow exists in the workspace yet, so packaging artifact paths remain a current gap.
  - The existing README documents the monorepo build convention from the repo root with `mvn -pl <module> -am ...`, which aligns with the current workflow pattern.
research_metadata:
  methodology: "memory kickoff notes + semantic_search + grep_search + targeted read_file + exact file metadata via wc -l"
  scope: "Current GitHub Actions workflow pattern, Maven module composition, documented build conventions, and approved fun_sejmapi kickoff constraints"
  confidence: "high"
  coverage: 91
  decision_blockers: 2
  research_blockers: 1
files_analyzed:
  - file: "pom.xml"
    path: "/sejmstream/pom.xml"
    purpose: "Monorepo root Maven aggregator and source of module membership required for module-scoped CI commands"
    key_elements:
      - element: "modules"
        type: "pattern"
        location: "/sejmstream/pom.xml:16"
        description: "Root aggregator currently lists only `fun_sejmlive`, which means `mvn -pl fun_sejmapi -am ...` cannot succeed until the module is added."
        language: "xml"
      - element: "packaging"
        type: "variable"
        location: "/sejmstream/pom.xml:12"
        description: "Root POM uses `pom` packaging, so job-scoped module builds depend on module registration under this aggregator."
        language: "xml"
    lines: 20
  - file: "fun_sejmlive/pom.xml"
    path: "/sejmstream/fun_sejmlive/pom.xml"
    purpose: "Reference module for inherited Maven structure, test dependencies, and current Java version mismatch against Azure Functions constraints"
    key_elements:
      - element: "parent"
        type: "pattern"
        location: "/sejmstream/fun_sejmlive/pom.xml:3"
        description: "The existing module inherits from the monorepo root POM using a relative parent, establishing the current module composition pattern."
        language: "xml"
      - element: "java.version"
        type: "variable"
        location: "/sejmstream/fun_sejmlive/pom.xml:14"
        description: "The existing module pins Java 25, which kickoff notes explicitly mark as invalid for the planned Azure Functions module."
        language: "xml"
      - element: "spring-boot-starter-test"
        type: "pattern"
        location: "/sejmstream/fun_sejmlive/pom.xml:36"
        description: "The current module includes a conventional Maven/JUnit test dependency, showing that CI today is centered on `mvn test`."
        language: "xml"
    lines: 114
  - file: ".github/workflows/fun_sejmlive-ci.yml"
    path: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml"
    purpose: "Authoritative in-repo example of module-scoped CI workflow shape"
    key_elements:
      - element: "paths filters"
        type: "pattern"
        location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:7"
        description: "The workflow runs only when module files, root `pom.xml`, or the workflow file itself change."
        language: "yaml"
      - element: "setup-java"
        type: "pattern"
        location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:28"
        description: "The current CI uses `actions/setup-java@v4` with Temurin and Maven dependency cache."
        language: "yaml"
      - element: "retry-wrapped module test command"
        type: "pattern"
        location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:36"
        description: "The build step wraps `mvn -B -q -pl fun_sejmlive -am test` in a three-attempt retry loop."
        language: "yaml"
      - element: "artifact upload"
        type: "pattern"
        location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:58"
        description: "Surefire reports are uploaded unconditionally and the built jar is uploaded on success."
        language: "yaml"
    lines: 72
  - file: "docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md"
    path: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md"
    purpose: "Approved scope and runtime constraints for the planned fun_sejmapi module"
    key_elements:
      - element: "dedicated CI coverage decision"
        type: "pattern"
        location: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:18"
        description: "Kickoff explicitly requires dedicated CI for `fun_sejmapi` instead of expanding the existing workflow."
        language: "markdown"
      - element: "Java 21 runtime constraint"
        type: "pattern"
        location: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:23"
        description: "The planned Azure Functions module must target Java 21."
        language: "markdown"
      - element: "module creation decision"
        type: "pattern"
        location: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:15"
        description: "The module is expected to be a separate Maven project inside the monorepo."
        language: "markdown"
    lines: 40
  - file: "README.md"
    path: "/sejmstream/README.md"
    purpose: "Repository-level documentation of the current module build convention from the monorepo root"
    key_elements:
      - element: "root build/test command"
        type: "pattern"
        location: "/sejmstream/README.md:30"
        description: "The README documents the root-level pattern `mvn -pl fun_sejmlive -am test`, matching the CI workflow invocation style."
        language: "markdown"
      - element: "monorepo scope"
        type: "pattern"
        location: "/sejmstream/README.md:18"
        description: "The README currently documents only `fun_sejmlive` as the application module."
        language: "markdown"
    lines: 34
patterns_found:
  - category: "structure"
    pattern: "One workflow per module"
    description: "The current repo uses a dedicated workflow file named after the module, rather than a shared matrix workflow."
    examples:
      - file: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml"
        location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:1"
        snippet: "name: fun_sejmlive CI"
    prevalence: "common"
  - category: "architecture"
    pattern: "Path-filtered workflow triggering"
    description: "CI is scoped to a single module plus root Maven metadata and the workflow file itself."
    examples:
      - file: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml"
        location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:7"
        snippet: "paths: fun_sejmlive/**, pom.xml, .github/workflows/fun_sejmlive-ci.yml"
    prevalence: "common"
  - category: "testing"
    pattern: "Module-scoped Maven invocation from repo root"
    description: "Both docs and CI use `mvn -pl <module> -am test` from the monorepo root to build module dependencies and run tests."
    examples:
      - file: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml"
        location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:43"
        snippet: "mvn -B -q -pl fun_sejmlive -am test"
      - file: "/sejmstream/README.md"
        location: "/sejmstream/README.md:30"
        snippet: "mvn -pl fun_sejmlive -am test"
    prevalence: "common"
  - category: "error_handling"
    pattern: "Inline retry loop for flaky CI execution"
    description: "The current workflow retries the module test command up to three times within a Bash loop instead of using a matrix or external action."
    examples:
      - file: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml"
        location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:39"
        snippet: "max_attempts=3"
    prevalence: "common"
  - category: "naming"
    pattern: "Module directory and workflow names are aligned"
    description: "The workflow file, artifact names, and Maven `-pl` selector all use the module directory name as the primary identifier."
    examples:
      - file: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml"
        location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:62"
        snippet: "name: fun_sejmlive-surefire-reports"
    prevalence: "common"
related_architecture:
  components_relevant_to_domain:
    - component: "Monorepo root Maven aggregator"
      responsibility: "Registers buildable modules and enables `-pl <module> -am` invocations from the repository root"
      location: "/sejmstream/pom.xml:16"
      relationship_to_domain: "Controls whether the CI workflow can target `fun_sejmapi` as a module"
    - component: "fun_sejmlive module"
      responsibility: "Current example Java module and current source of build/test conventions"
      location: "/sejmstream/fun_sejmlive/pom.xml:3"
      relationship_to_domain: "Provides the nearest module-level template, but with a Java runtime that does not apply to Azure Functions"
    - component: "fun_sejmlive GitHub Actions workflow"
      responsibility: "Defines the current CI shape for module build, test, retry handling, and artifact upload"
      location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:1"
      relationship_to_domain: "Primary in-repo pattern for the planned `fun_sejmapi` workflow"
    - component: "fun_sejmapi kickoff plan note"
      responsibility: "Defines scope and runtime constraints for the future module"
      location: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:15"
      relationship_to_domain: "Constrains CI inputs such as Java version, dedicated workflow ownership, and packaging intent"
  interfaces_used_by_domain:
    - interface: "GitHub Actions workflow triggers and upload-artifact action"
      location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:3"
      usage_pattern: "Triggered on push and pull_request to main with path filters; publishes test and build artifacts via `actions/upload-artifact@v4`"
    - interface: "Maven reactor module selection"
      location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:43"
      usage_pattern: "Uses `mvn -pl <module> -am test` to build the selected module from the root reactor"
  data_flow_involving_domain: "Git push or pull request touching module-scoped paths triggers a module-scoped workflow, which checks out the repo, provisions a Java toolchain with cached Maven dependencies, executes the module test phase from the root reactor, and uploads test/build outputs from the target directory."
  key_relationships_to_domain:
    - from: "GitHub Actions workflow"
      to: "Root Maven aggregator"
      relationship: "calls"
    - from: "Root Maven aggregator"
      to: "fun_sejmlive module"
      relationship: "composes"
    - from: "fun_sejmapi kickoff decisions"
      to: "Planned fun_sejmapi workflow"
      relationship: "calls"
related_technology_stack:
  languages_used_in_domain:
    - "YAML"
    - "XML"
    - "Markdown"
    - "Shell"
  frameworks_used_in_domain:
    - name: "GitHub Actions"
      usage_in_domain: "Hosts the module-scoped CI workflow"
    - name: "Maven Reactor"
      usage_in_domain: "Builds and tests individual modules from the monorepo root"
  libraries_used_in_domain:
    - name: "actions/checkout@v4"
      purpose_in_domain: "Checks out repository contents before the module build"
    - name: "actions/setup-java@v4"
      purpose_in_domain: "Installs Temurin JDK and enables Maven cache"
    - name: "actions/upload-artifact@v4"
      purpose_in_domain: "Publishes surefire reports and packaged outputs from the module target directory"
  external_apis_used_in_domain:
    - name: "GitHub Actions runner environment"
      integration_point: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:20"
related_conventions:
  naming_patterns_in_domain: "Workflow names, workflow filenames, artifact names, and Maven `-pl` selectors are module-derived and use the module directory name as the stable identifier."
  structure_of_domain: "Each module is expected to live under a top-level directory and be wired into the root reactor; each module can have a dedicated workflow in `.github/workflows`."
  error_handling_in_domain: "Transient CI instability is handled inside the workflow step via a bounded shell retry loop rather than by splitting compile/test into separate jobs."
  testing_in_domain: "Current CI executes Maven `test`; test evidence is preserved through `target/surefire-reports`."
  documentation_in_domain: "Build/test commands are documented in the root README and mirrored closely by the CI workflow."
related_dependencies:
  internal:
    - component: "Root pom.xml"
      relationship_to_domain: "Enables module-targeted reactor builds"
      direction: "outbound"
    - component: ".github/workflows/fun_sejmlive-ci.yml"
      relationship_to_domain: "Provides the existing workflow template and naming pattern"
      direction: "bidirectional"
    - component: "docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md"
      relationship_to_domain: "Defines the runtime and ownership constraints for the new workflow"
      direction: "inbound"
    - component: "README.md"
      relationship_to_domain: "Documents the root-level module build convention used by CI"
      direction: "inbound"
  external:
    - name: "Temurin JDK"
      purpose_for_domain: "Provides the Java runtime installed in CI"
    - name: "GitHub-hosted Ubuntu runner"
      purpose_for_domain: "Executes the shell-based Maven workflow"
    - name: "Maven Central and configured repositories"
      purpose_for_domain: "Resolve build and test dependencies during CI"
domain_security_considerations:
  sensitive_areas:
    - area: "Artifact publication"
      location: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:58"
      concern: "Current artifact publishing is limited to build outputs and test reports; no secret-bearing config files are currently included in upload paths."
    - area: "Future Azure Functions packaging"
      location: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:16"
      concern: "The planned Function App packaging will likely produce deployable contents; artifact path selection must avoid uploading local settings or environment-specific secrets once the module exists."
  authentication_patterns_in_domain: "No authentication or cloud deployment credentials are present in the current workflow; the existing pipeline is build/test only."
  authorization_patterns_in_domain: "The current workflow relies on standard repository Actions permissions implicit in checkout and artifact upload; no custom cloud authorization path is defined in this scope."
  data_validation_in_domain: "Path filters and module-specific artifact paths constrain the workflow to the intended module surface, reducing accidental cross-module execution."
testing_patterns:
  framework: "Maven Surefire with JUnit-oriented module tests"
  coverage_areas:
    - "Module-scoped `mvn test` execution from repo root"
    - "Surefire report retention in CI artifacts"
    - "Retry handling around the entire test command"
  test_organization: "Tests live under the module `src/test/java` tree and produce reports under `target/surefire-reports`."
  mock_patterns:
    - "CI preserves raw surefire output rather than transforming results into a separate report format"
open_questions:
  - question: "What exact packaged output path should be uploaded for Azure Functions in `fun_sejmapi`?"
    context: "The repo contains no `fun_sejmapi` module and no `azure-functions-maven-plugin` configuration yet, so the final packaging directory or zip path is not observable in the current codebase."
    type: "decision_blocker"
    affects:
      - "workflow_shape"
      - "artifacts"
      - "validation"
  - question: "Should the first workflow run `test` only, or also a packaging phase that materializes Azure Functions deployable artifacts?"
    context: "The existing workflow stops at `mvn test`, while the request also asks for uploadable artifacts relevant to Azure Functions packaging."
    type: "decision_blocker"
    affects:
      - "workflow_shape"
      - "job steps"
      - "artifact definitions"
  - question: "Will `fun_sejmapi` share the root Spring Boot parent or require a different parent/plugin stack for Azure Functions?"
    context: "The existing module inherits from the root Spring Boot parent, but the future Azure Functions module has different runtime constraints and likely different plugin needs."
    type: "research"
    affects:
      - "maven command semantics"
      - "files_to_modify"
  - question: "Should path filters include only the new workflow and module directory, or also shared infra/documentation files tied to Function packaging?"
    context: "Current pattern includes module path, root `pom.xml`, and the workflow file itself; no existing repo example covers Azure Functions packaging metadata."
    type: "nice_to_know"
    affects:
      - "trigger scope"
gaps:
  - area: "fun_sejmapi module implementation"
    description: "No `fun_sejmapi` directory or Maven module exists in the current workspace."
    impact: "decision_blocker"
    affects:
      - "CI executability"
      - "artifact path verification"
      - "workflow command selection"
  - area: "Azure Functions packaging pattern in-repo"
    description: "No `azure-functions-maven-plugin`, `host.json`, or comparable Azure Functions build output exists in the workspace."
    impact: "research_blocker"
    affects:
      - "artifact upload paths"
      - "packaging validation"
  - area: "Repository planning anchors"
    description: "`AGENTS.md` and `docs/PRD.yaml` do not exist, so the research had to rely on kickoff notes and current code patterns instead of canonical planning anchors."
    impact: "nice_to_know"
    affects:
      - "requirements traceability"
workflow_shape:
  trigger_scope:
    observed_pattern: "push and pull_request to main with module path, root pom.xml, and workflow file path filters"
    source: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:3"
    fun_sejmapi_equivalent_surface:
      - "fun_sejmapi/**"
      - "pom.xml"
      - ".github/workflows/fun_sejmapi-ci.yml"
  job_topology:
    observed_pattern: "single `build-and-test` job on `ubuntu-latest`"
    source: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:19"
  toolchain:
    observed_pattern: "`actions/setup-java@v4` with Temurin and Maven cache"
    source: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:28"
    fun_sejmapi_constraint: "Java 21 required by kickoff decision"
    constraint_source: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:23"
  build_test_step:
    observed_pattern: "retry-wrapped root-level Maven invocation using `-pl <module> -am test`"
    source: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:36"
    executable_breakdown:
      - "Checkout repository"
      - "Set up Temurin JDK with Maven cache"
      - "Execute module-scoped Maven command from repo root under a bounded retry loop"
      - "Upload surefire reports regardless of outcome"
      - "Upload packaged module artifacts on success"
  artifacts:
    observed_pattern:
      - name: "surefire reports"
        path: "fun_sejmlive/target/surefire-reports/**"
        source: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:62"
      - name: "built jar"
        path: "fun_sejmlive/target/*.jar"
        source: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:70"
    fun_sejmapi_delta: "The request calls for artifacts relevant to Azure Functions packaging; equivalent output paths are not yet derivable from the current repo because the module does not exist."
files_to_modify:
  confirmed_required:
    - path: "/sejmstream/pom.xml"
      reason: "Root Maven aggregator must register `fun_sejmapi` before any `-pl fun_sejmapi -am ...` CI step can execute."
      source: "/sejmstream/pom.xml:16"
    - path: "/sejmstream/.github/workflows/fun_sejmapi-ci.yml"
      reason: "Kickoff requires dedicated CI coverage for the new module, and the existing repo pattern uses one workflow file per module."
      source: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:18"
  likely_required_once_module_exists:
    - path: "/sejmstream/fun_sejmapi/pom.xml"
      reason: "CI artifact and command shape depend on the module packaging plugin configuration and Java 21 target."
      source: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:23"
    - path: "/sejmstream/README.md"
      reason: "Root documentation currently names only `fun_sejmlive` and documents only its module-scoped build/test commands."
      source: "/sejmstream/README.md:18"
risks:
  - risk: "Workflow cannot be executable until the root Maven reactor knows about `fun_sejmapi`."
    evidence: "/sejmstream/pom.xml:16"
    impact: "High"
  - risk: "Reusing the current module runtime configuration would incorrectly carry Java 25 into an Azure Functions module that must use Java 21."
    evidence: "/sejmstream/fun_sejmlive/pom.xml:14 and /sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:23"
    impact: "High"
  - risk: "The current CI example uploads a jar, but Azure Functions packaging output may not be a jar-only artifact, so copying artifact paths blindly would produce incomplete or wrong uploads."
    evidence: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:68"
    impact: "High"
  - risk: "Because the repo has only one workflow and no shared CI abstraction, workflow duplication may cause drift between module pipelines over time."
    evidence: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:1"
    impact: "Medium"
validation:
  currently_observable:
    - check: "Existing workflow path filters are module-scoped and include root pom.xml"
      source: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:7"
    - check: "Existing CI command and README command both use repo-root `mvn -pl <module> -am test`"
      source: "/sejmstream/.github/workflows/fun_sejmlive-ci.yml:43 and /sejmstream/README.md:30"
    - check: "Kickoff constraint requires Java 21 for fun_sejmapi"
      source: "/sejmstream/docs/plan/20260603-fun-sejmapi/kickoff-decisions-20260603.md:23"
  planned_executable_validation_targets:
    - "Trigger only when `fun_sejmapi/**`, `pom.xml`, or `.github/workflows/fun_sejmapi-ci.yml` changes"
    - "From repo root, the workflow command resolves the module via Maven reactor selection"
    - "Surefire reports are present under the new module target directory after test execution"
    - "Azure Functions packaging artifact path exists and is uploaded after a successful package-oriented build step"